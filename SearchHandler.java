// Search through a MovieCollection given a query
// Pass in a MovieCollection and a query
// Return a smaller MovieCollection of Movies that have terms that match the query

public static MovieCollection search(MovieCollection inputCollection, String query) {
 
}
