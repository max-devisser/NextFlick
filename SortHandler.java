// Sort a movie collection based on a parameter
// Pass in a MovieCollection and a sort parameter (check to make sure that what they want to sort by makes sense, 
//   e.g. don't sort by plot or posterURL)
// Returns sorted MovieCollection

public static MovieCollection sort(MovieCollection inputCollection, String parameter) {
   
}
